# Explorer Oreo — leaderboard backend URL (optional).
# Copy to .env and set to your deployed Cloudflare Worker URL (see server/README.md).
# If unset, the game runs offline on localStorage with no errors.
VITE_API_URL=https://explorer-oreo-leaderboard.<you>.workers.dev
